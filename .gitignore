.worktrees/
.private-journal/

# AI Stuff
.claude-flow/
.swarm/

# Wrangler runtime logs
.wrangler/logs/

# Node.js
node_modules/
npm-debug.log*

# TypeScript build output
# Note: mcp/dist/ is committed for marketplace plugin distribution
# so the plugin works without requiring npm install
# Source maps are excluded to reduce diff noise (not needed by consumers)
mcp/dist/*.map
mcp/dist/**/*.map
mcp/dist/*.d.ts.map
mcp/dist/**/*.d.ts.map
*.tsbuildinfo

# Testing
coverage/
.jest-cache/
test-temp/

