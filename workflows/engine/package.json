{
  "name": "@wrangler/workflow-engine",
  "version": "0.1.0",
  "description": "Deterministic workflow engine for spec implementation using Claude Agent SDK",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "wrangler-workflow": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "node --experimental-vm-modules node_modules/.bin/jest",
    "test:coverage": "node --experimental-vm-modules node_modules/.bin/jest --coverage",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.39",
    "commander": "^12.0.0",
    "fast-glob": "^3.3.0",
    "fs-extra": "^11.2.0",
    "gray-matter": "^4.0.3",
    "picomatch": "^4.0.3",
    "yaml": "^2.4.0",
    "zod": "^4.0.0",
    "zod-to-json-schema": "^3.25.1"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.0",
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "@types/picomatch": "^4.0.2",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  }
}
